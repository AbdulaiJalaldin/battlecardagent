to run this code
first install the dependancies in requirements.txt 
pip install -r requirements.txt

create a .env file and get your api keys for
FIRECRAWL_API_KEY=
OPENAI_API_KEY or claude
run main.py so the agent can scrape the data and generate the results
then finally run streamlit app.py